1. Алгоритм и структура программы:

Программа состоит из двух классов: Vect (для представления векторов) и Mat (для представления матриц). Каждый класс содержит соответствующие поля для хранения размерностей, значений и уникального номера объекта. Также в каждом классе присутствует статическое поле count для отслеживания количества созданных объектов.

Класс Vect:
- Поля: dim (размерность вектора), b (динамический массив для хранения значений вектора), num (уникальный номер объекта), count (статическое поле для отслеживания количества созданных объектов).
- Конструктор принимает размерность и массив значений для инициализации вектора.
- Деструктор освобождает динамически выделенную память и выводит сообщение об удалении объекта.
- Реализованы конструктор копирования, оператор присваивания и оператор-функции для операций векторной алгебры: +, -, унарный -, скалярное произведение, умножение на скаляр.
- Дружественная функция для умножения скаляра на вектор.

Класс Mat:
- Поля: rows (количество строк), cols (количество столбцов), a (двумерный динамический массив для хранения значений матрицы), num (уникальный номер объекта), count (статическое поле для отслеживания количества созданных объектов).
- Конструктор принимает количество строк, столбцов и одномерный массив значений для инициализации матрицы.
- Деструктор освобождает динамически выделенную память и выводит сообщение об удалении объекта.
- Реализованы конструктор копирования, оператор присваивания и оператор-функции для операций с матрицами: +, -, унарный -, умножение матриц, умножение на скаляр.
- Дружественные функции для умножения скаляра на матрицу и умножения матрицы на вектор.

2. Функция main:

В функции main создаются объекты классов Vect и Mat и демонстрируется работа реализованных операций. Выводятся сообщения о создании, удалении объектов и выполнении операций с участием соответствующих номеров объектов.

3. Вывод программы:

При запуске программы будут выведены сообщения о создании, удалении объектов и выполнении операций с участием соответствующих номеров объектов. Также будут напечатаны результаты операций с векторами и матрицами.

Программа демонстрирует реализацию описанных классов и соответствующих операций с выводом сообщений о выполняемых действиях и номерах участвующих объектов.
